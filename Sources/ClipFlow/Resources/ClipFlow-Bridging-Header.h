#ifndef CLIPFLOW_BRIDGING_HEADER_H
#define CLIPFLOW_BRIDGING_HEADER_H

// libmpv。头文件路径由工程里的 HEADER_SEARCH_PATHS 提供，
// 同时列了 Apple Silicon 与 Intel 两个 Homebrew 前缀。
#include <mpv/client.h>
#include <mpv/render.h>
#include <mpv/render_gl.h>

#endif
